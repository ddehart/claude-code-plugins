---
genitor: "[[entities]]"
tags: [skill, dev-workflow]
---

# dev-workflow spec-writing skill

The interview-driven spec skill used to finalize the knowledge-commons spec via `AskUserQuestion`. Runs in
the main context (not `context: fork`) and produces an implementation-ready spec. In this session it fed
the single neutral "can you build this?" cold read rather than an adversarial audit loop; see
[[break the self-review chain with a different agent]].

## interaction log
- 2026-07-15 — used to converge the knowledge-commons spec at reference weight after the reference was read.
