---
genitor: "[[entities]]"
tags: [skill, knowledge-commons, generated]
---

# the generated knowledge-graph skill

The project-owned skill emitted by [[graph-init skill]] that defines a target graph's note shapes and
conventions: navigation (atlas → maps → notes via `genitor:` backlinks and annotated alphabetical maps),
the frontmatter contract, extraction and judgment workflows written as prose from the interview, and the
one structural rule that is close to load-bearing — every evidence note must support at least one attractor
(bidirectional `supports:` field plus a line in the attractor's `## evidence`). Owned by the target project
and expected to drift, exactly like a hand-written skill.

## interaction log
- 2026-07-15 — this graph's own instance; its conventions doc's bare-heading map example was reproduced by
  the instantiator and fixed at the root.
