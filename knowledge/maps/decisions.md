---
genitor: "[[principium]]"
tags: [map]
---

# Decisions

Settled attractors — design choices made for this project's plugins with the reasoning attached. Each
entry is annotated with the decision's upshot: `- [[title]] — what was chosen and why, in one clause`.

- [[knowledge-commons ships at the weight of the system it generalizes]] — no validator/transactions/lifecycle; scope filtered by "has the reference needed this in a year of real use?"
- [[processing is local to each domain and the commons is receive-only via promotion]] — domains process at home; `/promote` is the commons's only inlet, uniform and `domain:`-stamped.
- [[promotion is scheduled into the process pipeline, never left to memory]] — the process template asks "does this generalize?" so promotion never depends on remembering.
- [[the commons is the evidence tier beneath behavioral rules]] — consulted on demand, never ambiently injected; principles graduate to rules with approval and cross-domain evidence.
- [[the knowledge-commons plugin generates project-owned prose, not a runtime engine]] — config drives shapes, interview fills procedures; generated skills are target-owned and expected to drift.
- [[patches are semantic instructions anchored on headings, never diffs]] — headings survive generation byte-identical while bodies diverge; that asymmetry is the whole design.
- [[session transcripts are primary and the chronicle is the lossy fallback]] — **superseded 2026-07-28**; kept as design history. Treated the chronicle as a competing source tier, which is the arrangement the correction removes.
- [[entity recommendation belongs at the type layer, not the instance layer]] — the ask was schema, not notes; five reviews couldn't see it because none checked intent against request.
- [[in-place rewrite of a shipped delta requires that nothing has stamped it]] — the append-only rule protects recorded state; where none exists there is nothing to protect.
- [[processing a live session is not delegable to a subagent]] — the approval gate and the first-person reflection both require having been present.
- [[processing preserves the raw and synthesizes for a human, and extracts from the raw]] — four stages, not two; the synthesis is a sibling output of the raw, never the thing evidence is extracted through.
- [[raw that cannot be published needs a home the spec does not give it]] — D2 says raw commits in-repo and D7's withhold branch says it isn't archived at all; neither covers raw that must be kept and cannot be published.
