---
genitor: "[[principium]]"
tags: [map]
---

# Sources

Processed source material, keyed on a canonical `source:` (a chronicle file's repo-relative path, a
fetched URL). Each source note carries the `processed:` stamp that lets `/process` resume instead of
redoing. No notes yet.
