# Transient workflow state
.claude/cache/

# Derived session-transcript exports (process skill, session tier) — never committed
.claude/session-exports/

# Ephemeral git worktrees created by EnterWorktree
.claude/worktrees/
