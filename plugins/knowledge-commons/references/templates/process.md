---
name: process
description: <!-- SLOT: description — trigger-rich description naming this graph's real sources and the /process invocation. Drawn from interview block 1 (graph name) and block 3 (sources). Write 2-4 sentences: what this skill does, when it fires, and the concrete nouns a session would say that should trigger it (source types, the domain name, "process my chronicle", etc). Do not reuse this instruction text in the generated skill. -->
    Example (orchard, a project graph whose source is a chronicle directory): "Process new chronicle entries into the orchard knowledge graph — turns session notes into observations, updates the patterns and decisions they support, and proposes promotions to the personal commons. Use when the user says 'process the chronicle', 'run /process', mentions a chronicle file that hasn't been folded into the graph yet, or asks what's in the processing queue."
allowed-tools:
  - Skill
  - Agent
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Bash
---

# process

This is the orchestrator for the `{graph.name}` knowledge graph. It turns raw material — chronicle
entries, calls, threads, whatever this domain's sources are — into graph notes, via one human-approved
plan per run. It does not enforce anything beyond that approval: the graph's health rests on the
conventions in the sibling `knowledge-graph` skill and on the plan you read before it runs.

Values written in `{like this}` were filled from `.commons.yml` when this skill was generated by
`graph-init`. They are concrete names now, not placeholders — read them as this graph's actual type
names, directories, and sinks.

## 1. Entry

`/process <input>` takes a URL, a file path, or pasted text naming one piece of raw material to work
through.

`/process` with no argument enumerates the queue instead of processing anything: for each source tier
configured below that declares a `path:`/`glob:`, list every matching file that has no source note in
the graph carrying its canonical `source:`. List queue entries human-readably — a date and a short
preview (the first heading, the opening line, or the initial prompt), never a bare identifier: a queue
of UUIDs tells the reader nothing about what they're approving. Show the queue and stop — running the
queue is a separate, explicit invocation per item (or "process all of these"), not implied by listing
them. For tiers that
arrive by hand (a pasted URL, an attached document — nothing to glob), there is no queue: ask the user
to paste or attach the input instead.

## 2. Resolve

Match the input to one of this graph's source tiers and normalize it to a canonical `source:` identity —
the value the ledger will key on. A chronicle file's identity is its repo-relative path; a URL's identity
is the URL itself, normalized (strip tracking params, resolve redirects if the tier's resolver does that).
If the tier names a resolver skill, invoke it now to do the normalization and any fetch the tier needs.

<!-- SLOT: source-tiers — one subsection per source tier from .commons.yml's sources: list (interview
block 3). For each tier, state: what identifies an input as belonging to this tier, how its canonical
source: is derived, and what resolver skill (if any) is invoked and what it returns. If a tier has no
resolver (e.g. a chronicle file is already local text), say so plainly instead of inventing a step.
Two tier properties worth stating when the interview surfaces them: a conditional pre-step (e.g. raw
material that needs formatting before extraction — name the skill or agent and when it's skipped), and
batching granularity (a transactional tier may process at the entity level, batching several thin inputs
into one pass, rather than one input at a time — say so, and note that batch passes are treated as new
each time, with the human in the loop to catch re-proposals). If two tiers cover the SAME underlying
events (a session transcript and a chronicle entry describing that session), name which tier is primary
and state the guard: before extracting from the secondary, check the graph for what the primary already
captured from that event, and extract only what's new — the same observation arriving twice through two
tiers is the double-counting failure this line exists to prevent. -->
    Example (orchard has one tier, chronicle):
    **Chronicle.** Input is a path under `docs/chronicle/` matching `20*.md`, or bare text that names a
    date the queue can resolve to that path. Canonical `source:` is the repo-relative path
    (`docs/chronicle/2026-07-14.md`). No resolver skill — chronicle files are already local markdown;
    read them directly with the Read tool.

## 3. Find the ledger

Search the graph for the source note carrying this `source:`. Source notes are the only note type that
carries a `source:` field, so a targeted grep across the source directory is enough — do not build an
index for this.

- **Found:** enter **augment mode**. Read its `processed:` stamp and read the notes it already produced
  (the stamp's `ran:` list names them, or they can be found via the source note's outbound links). Work
  out what's new by reading the current content against what's already captured — never by hashing or
  diffing the raw file. Tell the user plainly what already exists rather than re-proposing it.
- **Not found:** this is a first pass. Proceed to inspection.
- **Fuzzy match** (a source note with a similar but not identical `source:`, e.g. a renamed file): stop
  and ask which is correct. Never guess at ledger identity — a wrong guess either duplicates history or
  silently merges two distinct inputs.

## 4. Inspect

Read the resolved input and decide what it contains, sized to the input:

- **Under ~1,500 words:** read it inline, in this conversation.
- **Above ~1,500 words:** fan out one subagent per signal class (below), each returning a structured list
  of findings for its class. Synthesize the returned lists yourself before moving to the plan — don't
  forward subagent output verbatim.

<!-- SLOT: signal-classes — the signal classes inspection looks for, per tier, from interview block 3
("what signal classes should inspection look for"). Name each class plainly (what it is, what a finding
in that class looks like) so both the inline-read path and the subagent-fanout path use the same
vocabulary. Keep it to what this domain actually has classes for — don't invent classes to fill space. -->
    Example (orchard, chronicle tier — three classes):
    - **Decisions.** A choice was made and a reason given — becomes a `decision` attractor or evidence
      for an existing one.
    - **Recurring friction.** The same kind of problem shows up again, named or not — becomes a
      `pattern` attractor once it has ≥1 piece of evidence, or evidence toward an existing pattern.
    - **Standalone observations.** True and useful on their own, not yet clustering with anything —
      becomes an `observation` with no attractor yet; note it as unattached in the plan rather than
      forcing a link.

## 5. Propose the plan

Lay out, per skill/note, what will be created and what will be updated — and just as importantly, what's
being skipped and why (already captured, too operational, not durable — see the sibling
`knowledge-graph` skill's conventions). Present it as `[y / edit / explain]`. One approval gates the
entire run; there is no per-note confirmation after this point except the re-pause condition in step 6.

## 6. Run to completion

Execute the approved plan in dependency order — notes that other notes will link to get written first
(an attractor before the evidence that cites it, unless the attractor already exists). Re-pause only for
genuine ambiguity or a conflict with the existing record (the plan said one thing and the graph, on
write, turns out to already say something incompatible). Creating a new entity, attractor, or note that
simply doesn't exist yet is not ambiguity — proceed without pausing.

<!-- SLOT: extraction-procedure — the domain's extraction procedure from interview block 5 (walk one
source through start to finish: what gets created, in what order, what else must update alongside it —
attractor evidence sections, entity notes, maps). This slot mostly DELEGATES: name the specific workflow
sections or headings in the sibling knowledge-graph skill that carry the actual write mechanics, rather
than restating note-writing mechanics here. This section says WHEN each thing happens during a run;
knowledge-graph.md says HOW to write it. -->
    Example (orchard):
    For each finding from step 4: if it supports an existing pattern or decision, follow
    `knowledge-graph.md`'s "Adding evidence to an attractor" workflow — write the observation, add it to
    the attractor's evidence section, update the attractor's map-entry annotation if the "so what"
    shifted. If it doesn't yet support anything, follow "Writing an unattached observation" and leave it
    for a future run to cluster. If three or more recent unattached observations share a theme, follow
    "Promoting a cluster to an attractor" instead of adding a fourth orphan.

## 7. Sinks

Route any non-graph output (tasks, tracker rows, anything that isn't a note) to the sink named for it,
never as a note in the graph.

<!-- SLOT: sink-specifics — one subsection per sink from .commons.yml's sinks: list (interview block 4).
For each: what kind of finding routes here, which skill is invoked, and what its approval mode means
concretely (per-item = it asks per row as its own gate — don't add a second confirmation on top of it;
batch = one combined confirmation before it runs; silent = fires without asking, state plainly why that's
safe for this sink if so configured). If a sink reads a graph note (e.g. a tracker row whose narrative
comes from a note written earlier in this run), state that ordering dependency here — the sink runs
after the note exists, never against a stale or missing one. -->
    Example (orchard, one sink, tasks):
    **Tasks.** A finding that names a concrete follow-up action (not a knowledge claim) routes here.
    Invoke the `todoist-followups` skill with the finding's text and source link. Its approval mode is
    `per-item` — it confirms each task with the user itself; this orchestrator does not add another
    confirmation on top of that.

## 8. Continue-and-collect

If a step in the approved plan fails, skip it and continue with the remaining independent steps rather
than aborting the run. Collect every failure and report them together at the end, with enough detail to
retry (what was attempted, what error came back), and offer to retry the failed steps now.

## 9. Stamp

Write or update the `processed:` stamp on the source note — a YAML list entry with `date:`, `ran:`
(what was actually written), `skipped:` (what was deliberately not written, and why), and `errored:`
(what failed, per step 8). It's a list so history accumulates across augment-mode runs rather than being
overwritten.

## 10. Report

Summarize created / updated / skipped / errored, then append a changelog entry following this graph's
own conventions — see the "Changelog" section of the sibling `knowledge-graph` skill for this project's
format. If the run surfaced something a skill *outside* the plan could act on, suggest it in the report
rather than auto-running it. (Generated skills reference each other directly by name; there is no `${CLAUDE_PLUGIN_ROOT}`
available here, because this skill lives in the project, not in the plugin.)

<!-- SLOT: promotion-tail — CONDITIONAL. Include this entire "## 11" section only if .commons.yml sets
promotes-to: for this graph. If the graph has no promotes-to:, omit section 11 entirely — do not leave a
placeholder or an empty header. When included, fill the generalization guidance from interview block 6
("what tends to generalize out of this domain, and what must never leave"). -->

## 11. The promotion tail

After the plan in step 5 is assembled — before presenting it — ask one more question of the run's
findings: does any of this generalize beyond `{graph.name}`? Candidates go **into the same plan**,
clearly marked as promotions, not into a separate pass someone has to remember to run.

The screen, applied to each candidate:

- **Strip the entities.** Remove every name, date, and domain-specific noun. Is there still a claim left,
  or was the "insight" just a fact about this instance?
- **Method over material.** A reusable way of thinking generalizes; a fact about this domain's specific
  material usually doesn't.
- **Non-obvious.** Would you tell your future self this, in a *different* domain, and have it be useful?
- **Not already steering.** Check the target (the commons, or `~/.claude/rules/` if promoting from the
  commons) for something that already says this before proposing it again.

When uncertain, propose anyway — a declined candidate costs one skip at plan review; a missed observation
is invisible damage that never gets a second chance.

<!-- SLOT: generalization-boundary — this domain's specific answer to "what tends to generalize out of
here, and what must never leave" (interview block 6). Make it concrete to the domain, not a restatement
of the screen above. -->
    Example (orchard): Timing and sequencing lessons ("do X before Y, not after") tend to generalize —
    they're method, not material. Specific quantities, prices, and supplier names never leave; they're
    true only because of this domain's particular constraints and are noise anywhere else.

Approved promotion candidates are executed by invoking this plugin's `promote` skill once per candidate,
after the rest of the plan has run — a promotion derives from a note that needs to exist first.
